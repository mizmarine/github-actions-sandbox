name: MyWorkflow
# This workflow is triggered on pushes to the repository.
on: [push]

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: pwd
          run: pwd
      - name: ls
          run: ls -la
      - name: test
        run: make test
